_Route15Gate2FOaksAideExpAllText::
	text "sina open e te ilo pana sona tawa monsuta ale to la" ; `EXP.ALL gives`
	line "monsuta sina ale li kama jo e nanpa sona." ; `EXP points to all`
	cont "monsuta li wile utala ala tawa kama suli." ; `the #MON with`
	;cont "" ; `you, even if they`
	;cont "" ; `don't fight.`

	para "taso nanpa sona li kama lili" ; `It does, however,`
	line "tawa monsuta ale." ; `reduce the amount`
	;cont "" ; `of EXP for each`
	;cont "" ; `#MON.`

	para "sina wile ala e ni la" ; `If you don't need`
	line "o poki e ilo ni lon ilo nanpa sina." ; `it, you should `
	;cont "" ; `store it via PC.`
	done

_Route15Gate2FBinocularsText::
	text "sina kepeken ilo lukin pi ma weka." ; `Looked into the`
	;line "" ; `binoculars.`

	para "sina lukin e nena lili lon telo suli." ; `It looks like a`
	;line "" ; `small island!`
	done
