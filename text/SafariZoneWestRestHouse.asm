_SafariZoneWestRestHouseScientistText::
	text "sina pana e kiwen tawa monsuta la" ; `Tossing ROCKs at`
	line "ona li ken weka tan ni." ; `#MON might`
	cont "taso sina ken alasa e ona kepeken pali lili." ; `make them run,`
	;cont "" ; `but they'll be`
	;cont "" ; `easier to catch.`
	done

_SafariZoneWestRestHouseCooltrainerMText::
	text "sina pana e moku monsuta tawa monsuta la" ; `Using BAIT will`
	line "ona li wile ala weka." ; `make #MON`
	;cont "" ; `easier to catch.`
	done

_SafariZoneWestRestHouseSilphWorkerFText::
	text "mi tawa mute lon ma ni." ; `I hiked a lot, but`
	line "taso mi lukin ala e monsuta pi wile mi." ; `I didn't see any`
	;cont "" ; `#MON I wanted.`
	done
