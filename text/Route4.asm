_Route4CooltrainerF1Text::
	text "ike a." ; `Ouch! I tripped`
	line "mi tawa pakala e monsuta kiwen." ; `over a rocky`
	cont "" ; `#MON, GEODUDE!`
	done

_Route4CooltrainerF2BattleText::
	text "mi kama lon tan ni:." ; `I came to get my`
	line "mi kama jo e monsuta soko." ; `mushroom #MON!`
	done

_Route4CooltrainerF2EndBattleText::
	text "ike a." ; `Oh! My cute`
	line "monsuta soko mi a." ; `mushroom #MON!`
	prompt

_Route4CooltrainerF2AfterBattleText::
	text "mi pilin e ni:" ; `There might not`
	line "soko ale li weka lon ni." ; `be any more`
	;cont "" ; `mushrooms here.`

	para "ken la mi kama jo e ona ale." ; `I think I got`
	;line "" ; `them all.`
	done

_Route4MtMoonSignText::
	text "nena suli mun" ; `MT.MOON`
	line "open pi nasin lupa" ; `Tunnel Entrance`
	done

_Route4SignText::
	text "nasin nanpa 4" ; `ROUTE 4`
	line "nena suli mun -" ; `MT.MOON -`
	cont "- ma tomo [telo:pi.jasima.]" ; `CERULEAN CITY`
	done
